# MoeKoeMusic 任务栏歌词插件 — 项目当前状态

> **对照文档：** `MoeKoeMusic_TaskbarLyrics_开发文档.md` v0.5（草案）
> **版本：** v0.3.1
> **更新日期：** 2026-06-08

***

## 图例

| 标记 | 含义 |
|------|------|
| ✅ | 已完成 |
| ⚠️ | 已实现但与文档方案不同（替代方案） |
| ❌ | 未实现 |
| 🔜 | 文档标注为未来扩展 |

***

## 1. 项目概述

| 项目 | 文档规划 | 当前状态 | 说明 |
|------|---------|---------|------|
| 独立 EXE 运行 | ✅ 要求 | ✅ 已完成 | 单进程独立运行 |
| 零侵入 MoeKoeMusic | ✅ 要求 | ✅ 已完成 | 不修改主程序任何文件 |
| C++20 | ✅ 要求 | ⚠️ 替代 | 实际使用 C++17（CMakeLists.txt 中 `CMAKE_CXX_STANDARD=17`） |
| CPU < 2% | ✅ 要求 | ✅ 已完成 | 仅在有变化时渲染 |
| 内存 < 20MB | ✅ 要求 | ✅ 已完成 | 轻量级实现 |
| WebSocket 监听端口 6520 | ✅ 要求 | ✅ 已完成 | 支持配置端口 |
| 常量集中管理 | ✅ 新增 | ✅ 已完成 | `constants.h` 统一管理所有魔数（端口、尺寸、消息号等） |
| 代码质量审查改进 | ✅ 新增 | ✅ 已完成 | 按进阶版代码审查报告逐项修复高/中优先级问题 |
| API 模式自动开启 | ✅ 新增 | ✅ 已完成 | 检测 MoeKoeMusic API 模式，自动开启并重启 |
| 长歌词跑马灯滚动 | ✅ 新增 | ✅ 已完成 | 6 状态机实现 bounce/loop/off 三种模式 |
| Z-order 三重防护 | ✅ 新增 | ✅ 已完成 | 创建/消息/定期三重 TOPMOST 恢复 |

***

## 2. 模块实现状态

### 2.1 歌词数据获取模块 (`websocket_client.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| WebSocket 连接 | ✅ 已完成 | 使用 ixwebsocket 库 |
| 接收 lyrics 消息 | ✅ 已完成 | 支持 JSON 数组格式和 KRC 字符串格式 |
| 接收 playerState 消息 | ✅ 已完成 | 解析 isPlaying / currentTime |
| 接收 welcome 消息 | ✅ 已完成 | 忽略处理 |
| 自动重连（指数退避） | ✅ 已完成 | 1s → 2s → 4s → 8s → 15s（上限），使用 constants.h 常量 |
| 连接状态回调 | ✅ 已完成 | `OnConnectionStatus` 回调 |
| 发送控制指令 | ✅ 已完成 | `SendControl("toggle"/"next"/"prev")` |
| KRC 格式解析 | ⚠️ 替代 | 文档未规划，实际 MoeKoeMusic 推送的是 KRC 格式字符串，在 `ParseKrc()` 中实现 |
| 调试日志 | ✅ 已完成 | 改为可移植路径：获取 exe 目录，写入同目录 `debug.log` |
| 减少冗余日志 | ✅ 已完成 | 移除逐行 KRC 处理、逐字节消息内容等噪音日志，仅保留关键状态和错误 |
| 协作式线程退出 | ✅ 已完成 | 移除 TerminateThread，改为纯 join() + stopRequested_ 轮询（50ms 间隔） |
| WS 消息大小限制 | ✅ 已完成 | 入口处检查 raw.size() > MAX_WS_MESSAGE_SIZE(1MB)，超限丢弃并记录日志 |
| API 自动开启集成 | ✅ 已完成 | 第 3 次连接失败时调用 ApiEnabler::TryEnableApi() 自动检测并开启 MoeKoeMusic API 模式 |

### 2.2 HTTP 服务器模块 (`http_server.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| HTTP 服务监听 | ✅ 已完成 | 端口 6523，用于 Chrome Extension popup.js 通信 |
| 静态文件服务 | ✅ 已完成 | 提供 settings.html / icon.png 等 |
| 协作式线程退出 | ✅ 已完成 | 移除 TerminateThread，改为纯 join() + select() 轮询 stopRequested_ |

### 2.3 任务栏嵌入窗口模块 (`taskbar_window.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 查找 Shell_TrayWnd | ✅ 已完成 | `FindTaskbarHandle()` |
| 创建 Layered Window | ✅ 已完成 | WIC + UpdateLayeredWindow |
| 嵌入任务栏内部 | ⚠️ 替代 | 文档规划 `SetParent` 嵌入为子窗口；实际采用**独立浮动窗口覆盖在任务栏上方**（类似 TranslucentTB），不使用 SetParent |
| 任务栏方位检测 | ✅ 已完成 | BOTTOM / TOP / LEFT / RIGHT |
| 任务栏子窗口枚举定位 | ✅ 已完成 | 枚举 MSTaskListWClass / TrayNotifyWnd / ReBarWindow32 等，寻找空闲区域 |
| DPI 感知 | ✅ 已完成 | Per-Monitor V2 |
| 任务栏尺寸变化监听 | ✅ 已完成 | 主动轮询 `CheckResize()` + WM_SETTINGCHANGE |
| WM_DPICHANGED 处理 | ✅ 已完成 | 重新定位 |
| WM_DISPLAYCHANGE 处理 | ✅ 已完成 | 重新定位 |
| 鼠标悬停检测 | ✅ 已完成 | WM_MOUSEMOVE + TrackMouseEvent(TME_LEAVE) |
| 鼠标离开检测 | ✅ 已完成 | WM_MOUSELEAVE |
| 按钮点击检测 | ✅ 已完成 | WM_LBUTTONDOWN + HitTestButton() |
| 悬停状态变化回调 | ✅ 已完成 | OnHoverChanged 回调，通过 PostMessage 立即触发重绘 |
| 拖动调整位置 | ✅ 已完成 | WM_LBUTTONDOWN 开始拖动 + WM_MOUSEMOVE 更新位置 + WM_LBUTTONUP 结束拖动，保存偏移到 config |
| 拖动视觉反馈 | ✅ 已完成 | 拖动时半透明背景 + 蓝色边框 |
| Z-order 保持（三重防护） | ✅ 已完成 | ①创建时 WS_EX_TOPMOST ②WM_ACTIVATE 全状态恢复 ③每 30 帧周期性强制断言 TOPMOST |
| 位置偏移配置 | ✅ 已完成 | 支持 PositionConfig.offsetX/Y，启动时加载，拖动时保存 |
| 尺寸常量化 | ✅ 已完成 | 使用 constants.h 中 LYRIC_HEIGHT_BASE_DP / MAX_LYRIC_WIDTH_BASE_DP 等 |

**嵌入方式替代说明：** 文档规划使用 `SetParent` 将歌词窗口嵌入为任务栏子窗口，但 Win11 的任务栏结构变化导致 SetParent 方案不稳定。实际采用**独立浮动窗口 + WS_EX_TOPMOST** 覆盖在任务栏上方，这是 TrafficMonitor、TranslucentTB 等成熟项目的通用做法。

### 2.4 歌词同步与解析模块 (`lyrics_parser.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 数据结构定义 | ✅ 已完成 | CharacterTiming / LyricLine / LyricsData / PlayerState / RenderState |
| UpdateLyrics | ✅ 已完成 | 线程安全（mutex 保护） |
| UpdatePlayerState | ✅ 已完成 | 线程安全 |
| GetCurrentRenderState | ✅ 已完成 | 二分查找当前行 + 逐字进度计算 |
| FindLineIndex | ✅ 已完成 | 二分查找 |
| LRC 解析（备用） | ✅ 已完成 | `ParseLRC()` 静态方法 |
| hasLyrics / isPlaying 查询 | ✅ 已完成 | |
| RenderState.isHovering | ✅ 已完成 | 新增字段，支持悬停控制按钮渲染 |
| RenderState.isDragging | ✅ 已完成 | 新增字段，支持拖动视觉反馈 |
| HoverButton 枚举 | ✅ 已完成 | 新增 Prev / PlayPause / Next |

### 2.5 渲染引擎模块 (`renderer.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| Direct2D 工厂初始化 | ✅ 已完成 | D2D1_FACTORY_TYPE_SINGLE_THREADED |
| DirectWrite 工厂初始化 | ✅ 已完成 | |
| WIC 位图渲染目标 | ✅ 已完成 | WIC + CreateWicBitmapRenderTarget |
| UpdateLayeredWindow 呈现 | ✅ 已完成 | PBGRA 格式 + AC_SRC_ALPHA |
| 卡拉 OK 逐字高亮 | ✅ 已完成 | 单 textLayout 获取 metrics + 计算居中左边缘 + PushAxisAlignedClip 像素级裁剪（修复居中对齐时高亮位置错误的问题） |
| 高亮跟随滚动偏移 | ✅ 已完成 | 跑马灯滚动时 clipRect 同步 textLeft 偏移量 |
| 翻译文本绘制 | ✅ 已完成 | 小号字体，居中对齐 |
| 歌词文本对齐 | ✅ 已完成 | 歌词和翻译都改为居中对齐 |
| 透明背景 | ✅ 已完成 | alpha=0 清除 |
| 悬停时极低 alpha 背景 | ✅ 已完成 | alpha≈1/255，防止鼠标穿透 |
| 悬停控制按钮绘制 | ✅ 已完成 | ⏮ ⏸/▶ ⏭ Unicode 符号 + 半透明圆角背景(alpha=0.15) |
| 配置热更新 | ✅ 已完成 | ApplySettings 重新初始化 |
| 渲染目标重建 | ✅ 已完成 | D2DERR_RECREATE_TARGET 时自动重建 |
| 跑马灯状态机 | ✅ 已完成 | 6 状态（Idle→Delay→ScrollLeft→PauseRight→ScrollRight→PauseLeft），支持 bounce/loop/off 三种模式 |
| 超长歌词加速 | ✅ 已完成 | 宽度 > 可用宽度×2 时最高 3 倍速 |
| 歌词切换自动重置 | ✅ 已完成 | 检测 lyricText 变化自动重置跑马灯状态机 |
| SetDrawingEffect 逐字着色 | ❌ 未实现 | 文档规划的方案，实际使用 PushAxisAlignedClip 裁剪方案替代 |
| 默认颜色修正 | ✅ 已完成 | highlightColor 默认值从 #FFFFFF 改为 #4CC2FF，normalColor 默认值从 #999999 改为 #333333 |
| UI 常量化 | ✅ 已完成 | TEXT_PADDING_X / TRANSLATION_FONT_SIZE_DELTA / BUTTON_SPACING 等 |
| 按钮文字格式缓存 | ✅ 已完成 | `btnFormat_` 作为类成员，Initialize/Resize 时创建一次，不再每帧重复创建 |

**卡拉 OK 修复说明：** 修复了高亮区域与居中歌词不对齐的 bug —— 改为先获取单个 textLayout 的 metrics，计算居中后文字的左边缘，再按逐字进度精确裁剪。

### 2.6 配置管理模块 (`config.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| JSON 配置加载/保存 | ✅ 已完成 | 使用 nlohmann/json |
| enabled 开关 | ✅ 已完成 | |
| auto_start 开关 | ✅ 已完成 | |
| 外观配置 | ✅ 已完成 | highlightColor / normalColor / normalOpacity / fontFamily / fontSize / enableKaraoke / enableTranslation |
| 跑马灯配置 | ✅ 已完成 | enableMarquee / marqueeMode / delayMs / pauseMs / speedPxPerSec |
| 位置配置 | ✅ 已完成 | 新增 PositionConfig 结构，支持 offsetX / offsetY |
| 高级配置 | ✅ 已完成 | websocketPort / refreshRateHz / debugLog |
| 注册表自启管理 | ✅ 已完成 | HKCU\Software\Microsoft\Windows\CurrentVersion\Run |
| 配置文件路径 | ✅ 已完成 | 标准路径 `%APPDATA%\MoeKoeTaskbarLyrics\config.json`，首次启动自动创建目录 |
| 自启动诊断日志 | ✅ 已完成 | RegOpenKeyExW / RegSetValueExW / RegDeleteValueW 各步骤完整日志输出 |
| 配置值范围验证 | ✅ 已完成 | Load() 后使用 std::clamp 校验 opacity(0~1)、fontSize(8~72)、port(1024~65535)、refreshRate(1~120) |
| 默认字体/字号/刷新率 | ✅ 已完成 | 华文细黑 / 20pt / 60 FPS（上限 120 FPS） |

### 2.7 API 自动开启模块 (`api_enabler.cpp/h`) — **v0.3.1 新增**

| 功能 | 状态 | 说明 |
|------|------|------|
| MoeKoeMusic 进程检测 | ✅ 已完成 | FindWindowW(Chrome_WidgetWin_1) + CreateToolhelp32Snapshot 双重检测 |
| electron-store 配置定位 | ✅ 已完成 | `%APPDATA%\moekoemusic\config.json` |
| apiMode 读取 | ✅ 已完成 | JSON 解析 settings.apiMode 字段 |
| apiMode 写入（原子操作） | ✅ 已完成 | 先写 .tmp 临时文件 → MoveFileEx 原子替换 |
| MoeKoeMusic 重启 | ✅ 已完成 | ShellExecuteW 启动新实例（支持 UAC 提权） |
| 防重复机制 | ✅ 已完成 | 静态标记 s_attempted，每运行周期只尝试一次 |
| WS 连接失败触发 | ✅ 已完成 | ReconnectLoop 第 3 次失败时调用 TryEnableApi() |

### 2.8 系统托盘模块 (`tray_icon.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 托盘图标创建 | ✅ 已完成 | 使用 resources/icon.ico（已替换为用户提供的 icon.jpg 转换后的图标） |
| 右键菜单 | ✅ 已完成 | |
| "启用歌词显示" 菜单项 | ✅ 已完成 | ID_MENU_ENABLE，可勾选 |
| "开机自动启动" 菜单项 | ✅ 已完成 | ID_MENU_AUTOSTART，可勾选 |
| "重新连接" 菜单项 | ✅ 已完成 | ID_MENU_RECONNECT |
| "设置" 菜单项 | ✅ 已完成 | ID_MENU_SETTINGS，打开 WebView2 设置窗口 |
| "退出" 菜单项 | ✅ 已完成 | ID_MENU_EXIT |
| Tooltip 歌词提示 | ✅ 已完成 | 鼠标悬停托盘图标时显示当前歌词（截断至 WINDOWS_TOOLTIP_MAX_LEN=127 字符） |
| 回调消息号常量化 | ✅ 已完成 | WM_USER+0x200 → WM_TRAY_CALLBACK (constants.h) |
| "解除绑定" 菜单项 | ❌ 未实现 | 绑定模式未实现，此菜单项无意义 |

### 2.9 WebView2 设置窗口模块 (`settings_window.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 完整模块 | ✅ 已完成 | 全新 WebView2 实现，替代 Win32 设置对话框 |
| WebView2 异步初始化 | ✅ 已完成 | ICoreWebView2Environment → ICoreWebView2Controller → ICoreWebView2 完整异步链 |
| 原生 COM 回调 | ✅ 已完成 | 不依赖 WRL RuntimeClass，手动实现 IUnknown |
| settings.html 页面 | ✅ 已完成 | 包含所有配置项（颜色选择器、字体选择、滑块等）+ 跑马灯控制 + 使用教程 |
| 双向 JSON 通信 | ✅ 已完成 | window.chrome.webview.postMessage 与 ICoreWebView2::PostWebMessageAsJson |
| 字体选择按钮 | ✅ 已完成 | 调用 Windows 原生 ChooseFontW 对话框 |
| PostMessage 显示对话框 | ✅ 已完成 | 避免 WebView2 事件处理里直接显示模态对话框导致的重入问题 |
| NavigationCompleted 事件 | ✅ 已完成 | 页面加载完成后发送 initConfig |
| HTML 存在性检查 | ✅ 已完成 | OnControllerReady 时检查 resources/settings.html 是否存在 |
| DevTools 开关 | ✅ 已完成 | 仅调试时打开（当前已关闭） |
| 初始化失败回退 | ✅ 已完成 | WebView2 初始化失败时回退到 Win32 ConfigDialog |
| 窗口居中显示 | ✅ 已完成 | 相对于桌面居中 |
| 窗口图标 | ✅ 已完成 | 使用 resources/icon.ico |
| WebView2 Bounds 设置 | ✅ 已完成 | 让 WebView2 填满窗口客户区 |
| WebView2 可见性 | ✅ 已完成 | 正确设置 IsVisible |
| "应用并保存"关闭窗口 | ✅ 已完成 | 保存配置后自动关闭设置窗口 |
| UTF-8/UTF-16 编码转换 | ✅ 已完成 | Utf8ToWide/WideToUtf8 辅助函数，修复乱码问题 |
| JSON 类型安全解析 | ✅ 已完成 | settings.html 中 event.data typeof 检查后再 JSON.parse |
| 跑马灯配置同步 | ✅ 已完成 | 双向传递 enableMarquee/marqueeMode/delay/pause/speed |

### 2.10 Win32 回退设置对话框 (`config_dialog.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 完整模块 | ✅ 已完成 | 作为 WebView2 初始化失败的回退方案 |
| 设置生效修复 | ✅ 已完成 | 修复了 ConfigDialog::Show() 始终返回 false 的 bug，导致 ApplySettings 不执行 |
| 线程局部变量模式 | ✅ 已完成 | 使用 GetDialogResult()/GetOnUnbind() 避免回调作用域问题 |

### 2.11 进程监控模块 (`process_monitor.cpp/h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 进程轮询监控 | ✅ 已完成 | 每 PROCESS_MONITOR_INTERVAL_MS(2000ms) 轮询目标进程 |
| 启动/退出事件通知 | ✅ 已完成 | 通过 WM_PROCESS_EXITED 消息通知主窗口 |
| 协作式线程退出 | ✅ 已完成 | 移除 TerminateThread，改为 running_.store(false) + join() |
| 绑定模式集成 | ⚠️ 部分 | 模块代码已实现，但 main.cpp 中绑定模式分支尚未接入（❌ 见 2.12） |

### 2.12 主程序入口 (`main.cpp`)

| 功能 | 状态 | 说明 |
|------|------|------|
| COM 初始化 | ✅ 已完成 | WIC 和 WebView2 需要 |
| DPI 感知声明 | ✅ 已完成 | Per-Monitor V2 |
| 单实例检查 | ✅ 已完成 | 命名 Mutex |
| 全局异常过滤器 | ✅ 已完成 | 写入 debug.log |
| 配置加载 | ✅ 已完成 | 从 %APPDATA% 加载，首次启动创建默认 |
| 系统托盘初始化 | ✅ 已完成 | |
| 禁用时简单消息循环 | ✅ 已完成 | 仅保留托盘等待重新启用 |
| 任务栏窗口创建 | ✅ 已完成 | |
| 渲染器初始化顺序修复 | ✅ 已完成 | 先 app.renderer = &renderer，再 ApplyRendererSettings(app)，修复启动时恢复默认设置的 bug |
| WebSocket 客户端启动 | ✅ 已完成 | |
| 歌词回调 → 显示窗口 | ✅ 已完成 | 收到有效歌词后 ShowWindow |
| 播放状态回调 | ✅ 已完成 | |
| 连接状态回调 | ✅ 已完成 | 更新托盘 Tooltip |
| 按钮点击 → SendControl | ✅ 已完成 | prev/toggle/next |
| 悬停变化 → 立即重绘 | ✅ 已完成 | WM_RENDER_UPDATE (WM_USER+0x300) 消息 |
| 拖动变化 → 保存配置 | ✅ 已完成 | 拖动结束时自动保存 position.offsetX/Y |
| WM_TIMER 帧驱动 | ✅ 已完成 | MIN_FRAME_INTERVAL_MS(15ms) 最小间隔，60 FPS 可配（最高 120 FPS） |
| WM_TIMER 异常恢复 | ✅ 已完成 | catch 后尝试 Shutdown+重新 Initialize 渲染器，失败则 PostQuitMessage |
| 配置变更回调同步 | ✅ 已完成 | onConfigChanged 里 *app.config = cfg 同步主 config 对象 |
| "设置"菜单项分支 | ✅ 已完成 | 先尝试 WebView2 SettingsWindow::Show()，失败时回退到 ConfigDialog::Show() |
| 5 阶段初始化注释 | ✅ 已完成 | 系统初始化→应用初始化→模块初始化→消息循环→清理退出 |
| 启动自启诊断日志 | ✅ 已完成 | [STARTUP] AutoStart=%s 日志输出 |
| 消息号常量化 | ✅ 已完成 | WM_TRAY_CALLBACK / WM_RENDER_UPDATE / WM_PROCESS_EXITED |
| HWND_TOPMOST 修正 | ✅ 已完成 | OnLyrics 中 HWND_TOP → HWND_TOPMOST |
| 绑定模式分支 | ❌ 未实现 | 无 ProcessMonitor 集成 |
| 独立模式分支 | ✅ 已完成 | 当前唯一工作模式 |

### 2.13 CMakeLists.txt 构建系统

| 功能 | 状态 | 说明 |
|------|------|------|
| WebView2 链接 | ✅ 已完成 | 添加 WebView2Loader.lib |
| NuGet 包路径 | ✅ 已完成 | 配置 NuGet WebView2 包路径 |
| 资源文件复制 | ✅ 已完成 | 构建后自动复制 resources/icon.ico 和 resources/settings.html 到输出目录 |
| WebView2Loader.dll 复制 | ✅ 已完成 | 构建后自动复制 WebView2Loader.dll 到输出目录 |
| z.dll (zlib) 复制 | ✅ 已完成 | 从 vcpkg installed 目录复制 z.dll 到输出目录（含 find_library 回退） |
| 程序图标资源 | ✅ 已完成 | app_icon.rc 编译为图标资源嵌入 EXE |
| api_enabler 编译 | ✅ 已完成 | 新增 api_enabler.cpp/h 源文件 |
| post-build 清理 | ✅ 已完成 | 移除了废弃的 ../MoeKoeMusic/plugins/extensions/ 复制脚本 |

### 2.14 全局常量定义 (`constants.h`)

| 功能 | 状态 | 说明 |
|------|------|------|
| 网络端口常量 | ✅ 已完成 | WEBSOCKET_LISTEN_PORT(6520) / HTTP_SERVER_PORT(6523) |
| 渲染尺寸常量 | ✅ 已完成 | LYRIC_HEIGHT_BASE_DP / MAX_LYRIC_WIDTH_BASE_DP / MIN_WINDOW_WIDTH 等 |
| UI 参数常量 | ✅ 已完成 | TEXT_PADDING_X / BUTTON_SPACING / BUTTON_BG_PADDING 等 |
| Windows 限制常量 | ✅ 已完成 | WINDOWS_TOOLTIP_MAX_LEN(127) |
| 自定义消息号 | ✅ 已完成 | WM_TRAY_CALLBACK / WM_RENDER_UPDATE / WM_PROCESS_EXITED |
| 进程监控参数 | ✅ 已完成 | PROCESS_MONITOR_INTERVAL_MS(2000) |
| WS 重连策略 | ✅ 已完成 | WS_CONNECTED_POLL_MS / RECONNECT_WAIT_GRANULARITY_MS / MAX_RECONNECT_ATTEMPTS 等 |
| 安全限制 | ✅ 已完成 | MAX_WS_MESSAGE_SIZE(1MB) |
| 线程参数 | ✅ 已完成 | THREAD_JOIN_TIMEOUT_MS(2000) |
| 跑马灯参数 | ✅ 已完成 | MARQUEE_DELAY_MS / MARQUEE_PAUSE_MS / MARQUEE_SPEED_PX_PER_SEC / MARQUEE_SPEEDUP_THRESHOLD 等 |

***

## 3. 文档规划 vs 实际实现 — 关键差异汇总

| # | 文档规划 | 实际实现 | 原因 |
|---|---------|---------|------|
| 1 | SetParent 嵌入任务栏子窗口 | 独立浮动窗口覆盖在任务栏上方 | Win11 任务栏结构变化，SetParent 方案不稳定；TranslucentTB 等成熟项目均使用浮动方案 |
| 2 | C++20 | C++17 | 项目实际设定为 C++17，功能上无影响 |
| 3 | SetDrawingEffect 逐字着色 | PushAxisAlignedClip 裁剪方案（已修复居中对齐问题） | 文档自身提到的备选方案，实现更简单可靠；已修复居中对齐时高亮位置错误的 bug |
| 4 | 绑定模式 + 进程监控 | 仅独立模式 | ProcessMonitor 模块代码已完成，但 main.cpp 中绑定模式分支未接入 |
| 5 | WS_EX_TRANSPARENT 鼠标穿透 | 不使用 WS_EX_TRANSPARENT | 需要接收鼠标消息以支持悬停控制按钮和拖动 |
| 6 | 日志写入 %TEMP%/硬编码 | 日志写入 exe 同级 debug.log（可移植） | 调试阶段方案，使用 GetModuleFileName 获取 exe 目录，避免硬编码路径 |
| 7 | "解除绑定"菜单项 | 无此菜单项 | 绑定模式未实现 |
| 8 | 手动编辑 JSON 配置 | WebView2 GUI 设置界面 | 已完整实现配置 GUI，包含颜色选择器、滑块、字体选择按钮 |
| 9 | 无拖动调整位置 | 支持拖动歌词窗口调整位置并保存到配置 | 用户需求新增功能 |
| 10 | 无 Z-order 保持逻辑 | 三重 TOPMOST 防护（创建/消息/周期性） | 修复点击任务栏/展开托盘时歌词被覆盖的问题 |
| 11 | TerminateThread 强制终止线程 | 协作式 join() 退出 | 代码审查发现的安全隐患，全部 3 处（ws/http/process）已移除 |
| 12 | 散落各处的魔数 | constants.h 集中管理 | 代码审查建议，提取端口/尺寸/消息号/UI参数等为命名常量 |
| 13 | 无异常恢复机制 | WM_TIMER catch 后尝试 Shutdown+Reinitialize | 代码审查建议，防止渲染器异常导致程序卡死 |
| 14 | 无配置值校验 | std::clamp 范围验证 | 代码审查建议，防止用户手动编辑 JSON 导致非法值 |
| 15 | 无长歌词滚动 | 跑马灯 6 状态机（bounce/loop/off） | 设计文档需求：短歌词居中不动、长歌词往返滚动、超长加速 |
| 16 | 无 API 自动开启 | api_enabler 模块自动检测/修改/重启 | 用户需求：WS 连接失败时自动开启 MoeKoeMusic 的 API 模式 |
| 17 | 图标取最小尺寸 | manifest icons 128 排第一 | MoeKoeMusic 按 Object.keys()[0] 取图标，需将最大尺寸放首位 |

***

## 4. 文档规划的未来功能扩展状态

| 功能 | 文档优先级 | 当前状态 |
|------|-----------|---------|
| 歌词滚动动画（平滑移动） | ⭐⭐⭐ | ✅ 已完成（跑马灯状态机） |
| 双行歌词显示（上一行+当前行） | ⭐⭐⭐ | ❌ 未实现 |
| 自定义字体和颜色（配置界面） | ⭐⭐ | ✅ 已完成（WebView2 完整 GUI） |
| Chrome Extension 开关 UI | ⭐⭐ | ✅ 已实现（native_host + popup，已修复 EXTENSION_ID 占位符问题） |
| 歌词搜索（WebSocket 无数据时） | ⭐ | ❌ 未实现 |
| 绑定模式高级检测 | ⭐⭐ | ⚠️ 部分（ProcessMonitor 代码已完成，待接入 main.cpp） |
| 支持其他播放器 | ⭐ | ❌ 未实现 |
| 绑定模式自动修复 | ⭐ | ❌ 未实现 |
| API 模式自动开启 | ⭐⭐ | ✅ 已完成（api_enabler 模块） |

***

## 5. 新增功能（文档未规划）

| 功能 | 说明 |
|------|------|
| 鼠标悬停控制按钮 | 悬停时显示 ⏮ ⏸/▶ ⏭ 按钮，点击发送 prev/toggle/next 控制指令 |
| 悬停时极低 alpha 背景 | alpha≈1/255 防止鼠标穿透透明区域导致按钮消失 |
| 悬停状态即时重绘 | hover 变化时通过 PostMessage 立即触发渲染，不等 WM_TIMER |
| KRC 格式解析 | 实际 MoeKoeMusic 推送 KRC 格式歌词，在 websocket_client.cpp 中实现 ParseKrc() |
| 全局异常过滤器 | SetUnhandledExceptionFilter 写入 debug.log |
| WebView2 设置界面 | 完整的 GUI 配置界面，替代手动编辑 JSON |
| 拖动调整位置 | 拖动歌词窗口直接调整位置，保存到 config |
| 拖动视觉反馈 | 拖动时半透明背景 + 蓝色边框 |
| Z-order 三重保持 | 创建时 TOPMOST + WM_ACTIVATE 全状态恢复 + 每 30 帧周期性强制断言 |
| 配置文件 %APPDATA% 标准路径 | 首次启动自动创建目录 |
| WebView2 回退机制 | WebView2 初始化失败时回退到 Win32 设置对话框 |
| 字体选择按钮 | 调用 Windows 原生 ChooseFontW 字体选择对话框 |
| 程序/插件图标更新 | 使用用户提供的 icon.jpg 转换为多尺寸 ico/png |
| EXE 图标资源 | app_icon.rc 将 icon.ico 编译进 EXE |
| Chrome Extension 通信 | HTTP 服务器(:6523) + native_host + popup.js |
| constants.h 常量管理 | 消除所有魔数，集中定义端口/尺寸/消息号/UI参数/跑马灯参数 |
| WM_TIMER 异常恢复 | 渲染器异常时自动重试，避免程序卡死 |
| WS 消息大小限制 | 1MB 上限，防止恶意大消息耗尽内存 |
| 按钮文字格式缓存 | btnFormat_ 类成员，避免每帧重复创建 IDWriteTextFormat |
| 配置值范围验证 | std::clamp 校验 opacity/fontSize/port/refreshRate |
| 协作式线程退出 | 所有后台线程移除 TerminateThread，改用 join() |
| **跑马灯长歌词滚动** | **6 状态机（Idle→Delay→ScrollLeft→PauseRight→ScrollRight→PauseLeft），支持 bounce/loop/off，超长歌词自动加速，高亮跟随滚动** |
| **API 模式自动开启** | **api_enabler 模块：进程检测 → 读取 electron-store → 写入 apiMode=on → ShellExecuteW 重启 MoeKoeMusic** |
| **默认配置优化** | **华文细黑 / 20pt / 60 FPS（最高 120 FPS）/ 刷新率滑块上限 120** |
| **manifest 图标优化** | **icons 对象 128 排首，MoeKoeMusic 取最大图标作为插件显示图标** |

***

## 6. 修复的关键 Bug 汇总

| Bug 描述 | 修复方案 |
|----------|---------|
| 启动时歌词显示恢复默认配置 | 调整初始化顺序：先 app.renderer = &renderer，再 ApplyRendererSettings(app) |
| 设置修改后不生效 | 修复 ConfigDialog::Show() 始终返回 false 的问题，同时修复 onConfigChanged 回调里主 config 未同步的问题 |
| 卡拉 OK 高亮与歌词不对齐 | 改为单 textLayout + 计算居中左边缘 + 精确 clip 宽度 |
| WebView2 设置窗口打不开 | 修复 WM_NCCREATE 返回值（必须返回 TRUE），添加 NavigationCompleted，HTML 存在性检查，回退机制 |
| 歌词字体显示乱码 | 修复 SendConfigToWebView 里直接用 std::wstring 迭代器构造的错误，统一使用 Utf8ToWide/WideToUtf8 |
| "Buffer is too small"断言 | 修复 wcscpy_s 调用，添加 LF_FACESIZE 缓冲区大小和截断处理 |
| 字体选择导致重入问题 | 使用 PostMessage(WM_PICK_FONT) 在消息循环里显示 ChooseFontW |
| 硬编码日志路径 | 改为 GetModuleFileName 获取 exe 目录 |
| 缺少 WebView2Loader.dll/z.dll | CMakeLists.txt 添加构建后自动复制 |
| EXTENSION_ID 占位符 | native_host/install_host.bat 改为交互式输入 |
| 默认颜色与设置不一致 | 统一默认值：highlightColor #4CC2FF，normalColor #333333 |
| 长时间运行退出卡死 | 给后台线程添加超时和强制终止（后续升级为协作式退出） |
| EXE 图标不显示 | 创建 app_icon.rc 资源文件编译 icon.ico 进 EXE |
| 无法开机自启 | 修复注册表操作诊断日志，确认 Run key 写入路径正确 |
| "[object Object]" JSON 错误 | settings.html 添加 event.data typeof 类型检查 |
| TerminateThread 安全隐患 | 全部 3 处(ws/http/process)移除，改用 join() 协作式退出 |
| 渲染器异常无恢复 | WM_TIMER catch 后尝试 Shutdown+Reinitialize，彻底失败则 PostQuitMessage |
| 点击任务栏图标歌词被覆盖 | OnLyrics 中 HWND_TOP → HWND_TOPMOST + WM_ACTIVATE 全状态恢复 + 每 30 帧周期性 TOPMOST 断言 |
| 长歌词截断无法阅读 | 跑马灯状态机：短歌词居中、长歌词往返滚动、超长歌词自动加速 |
| API 模式关闭导致连接失败 | api_enabler 模块自动检测并开启 MoeKoeMusic API 模式（含自动重启） |
| 插件图标显示过小 | manifest.json icons 对象 128 排首，确保 MoeKoeMusic 取最大图标 |
| 设置页刷新率上限 60 | settings.html refreshRate max 属性改为 120 |

***

## 7. 待完成事项

1. **绑定模式接入 main.cpp** — ProcessMonitor 模块代码已完成，需在 main.cpp 中添加绑定模式分支
2. **双行歌词显示** — 上一行 + 当前行
3. **歌词搜索** — WebSocket 无数据时提供搜索能力
4. **支持其他播放器** — 扩展协议支持更多播放器
5. **低优先级：硬编码颜色提取** — 将 renderer.cpp 中的颜色值提取到 constants.h colors 命名空间
6. **低优先级：统一 logging 系统** — 合并 DebugLog/ConfigDebugLog 为统一接口
